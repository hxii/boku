# Arguments

[← Back to README](/README.md)

!!! IMPORTANT
    Arguments are not working yet. This is a work in progress.

It is possible to pass arguments to tasks using key-value pairs via `-a/--arg`, e.g. `boku run mytaskfile key=value`.
