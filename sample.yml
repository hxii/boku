version: 0.0.1
description: Testing task
author: Paul Glushak <paul@glushak.net>

tasks:
  hello:
    run: echo "Hello World"
    save_output: hello

  again:
    depends_on:
      - hello
    run: echo "${hello} again!"

  edit:
    run: echo "My editor is ${env:EDITOR}"
    on_success: echo "My command was successful!"
